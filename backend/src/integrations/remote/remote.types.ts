export type RegisterSnapshot = {
  registerId: string;
  clientNumber: string;
  label: string;
  salesToday: number;
  ticketCount: number;
  cashSessionOpen: boolean;
  lastSync: string;
  currency: string;
};
