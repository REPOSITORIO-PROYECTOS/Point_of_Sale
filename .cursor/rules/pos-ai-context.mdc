---
description: Contexto POS — leer AGENTS.md y docs/ai antes de correr dev o build
alwaysApply: true
---

# Point of Sale — contexto para agentes

Antes de ejecutar comandos o modificar arquitectura, consultar:

- [`AGENTS.md`](../../AGENTS.md) — resumen agentes
- [`docs/README.md`](../../docs/README.md) — índice documentación
- [`docs/ai/dev-runbook.md`](../../docs/ai/dev-runbook.md) — runbook dev
- [`docs/casos-de-uso/01-desarrollo-diario.md`](../../docs/casos-de-uso/01-desarrollo-diario.md) — dev humano

## Dev (default)

Desde raíz del repo:

```powershell
npm run dev:stack
```

Requiere: Node 20+, Docker Desktop (AFIP). BD dev: `backend/storage/` (`cd backend && npm run db:init` primera vez).

## No confundir

- `services/afip/` = deploy wrapper; código Python en repo `servicio_afip` externo
- `backend/` = NestJS + SQLite; **no** lógica fiscal AFIP
- Build `.exe` ≠ dev; usar `dist:win` solo si el usuario pide instalador

## Puertos

58173 frontend | 58001 api | 58086 afip | 58090 relay | 58174 portal remoto

Ver `config/ports.json`.
